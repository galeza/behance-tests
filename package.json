{
  "name": "behance-tests",
  "version": "1.0.0",
  "description": "Interview repo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cypress": "^6.3.0"
  }
}
