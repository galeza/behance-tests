{
  "name": "behance-tests",
  "version": "1.0.0",
  "description": "Interview repo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cypress": "^6.3.0",
    "prettier": "2.2.1",
    "pretty-quick": "^3.1.0"
  }
}
